{
  "name": "descall-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "echo 'Install handled by Render Blueprint'",
    "start": "echo 'Use render.yaml configuration'",
    "setup:render": "node scripts/setup-render-env.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
