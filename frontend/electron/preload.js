/**
 * Legacy stub — the packaged Electron app uses `preload.cjs`.
 * Kept only so accidental packaging of this file fails loudly.
 */
console.error(
  '[Descall] electron/preload.js is a legacy stub. Use electron/preload.cjs.'
);
