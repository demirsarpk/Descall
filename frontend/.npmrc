# Desktop Electron binary is not needed for Vercel web builds.
# Without this, `npm install --include=dev` downloads ~100MB+ and often
# fails with "socket hang up" on Vercel.
electron_skip_binary_download=1
